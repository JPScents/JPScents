# Local Docker-backed Supabase and Prisma configuration.
# Copy to .env and populate the publishable key from `npm run supabase:status`.
# Never add a service-role key to browser code or source control.
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:56322/postgres"
NEXT_PUBLIC_SUPABASE_URL="http://127.0.0.1:56321"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="replace-with-local-publishable-key"
