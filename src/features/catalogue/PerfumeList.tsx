import Link from "next/link";
import Image from "next/image";
import { siteConfig } from "@/config/site";
import type { CatalogueFilters } from "./catalogue";

const control = "h-10 border border-jp-admin-border bg-white px-3 text-sm";
type Perfumes = Awaited<ReturnType<typeof import("./catalogue").listAdminPerfumes>>;

function ImageCell({ url }: { url?: string }) { return <span className="flex h-14 w-12 shrink-0 items-center justify-center bg-jp-stone text-center text-[10px] text-jp-text-secondary">{url ? <Image unoptimized src={url} alt="" width={96} height={112} className="h-full w-full object-contain" /> : "No image"}</span>; }

export function PerfumeList({ perfumes, filters }: { perfumes: Perfumes; filters: CatalogueFilters }) {
  const active = Boolean(filters.query || filters.availability === "available" || filters.availability === "unavailable" || filters.placement === "featured" || filters.placement === "bestseller");
  return <section className="space-y-6"><header className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-[11px] font-semibold uppercase tracking-[.14em] text-jp-text-secondary">Perfumes</p><h1 className="mt-1 font-display text-5xl leading-none">Manage the catalogue.</h1><p className="mt-3 text-sm text-jp-text-secondary">Update perfume details, variants, quantities, images, and placement.</p></div><Link href={siteConfig.routes.adminNewPerfume} className="inline-flex h-11 items-center justify-center bg-jp-admin-action px-5 text-sm font-semibold text-white">Add perfume</Link></header>
    <form className="flex flex-col gap-3 border border-jp-admin-border bg-jp-admin-surface p-4 md:flex-row md:items-center"><label className="sr-only" htmlFor="query">Search perfumes</label><input id="query" name="query" defaultValue={filters.query} placeholder="Search perfumes" className={`${control} min-w-0 flex-1`} /><select aria-label="Availability" name="availability" defaultValue={filters.availability ?? "all"} className={control}><option value="all">Availability · All</option><option value="available">Available</option><option value="unavailable">Unavailable</option></select><select aria-label="Homepage placement" name="placement" defaultValue={filters.placement ?? "all"} className={control}><option value="all">Homepage · All</option><option value="featured">Featured</option><option value="bestseller">Bestseller</option></select><button className="h-10 border border-jp-admin-action px-4 text-sm font-semibold" type="submit">Apply</button>{active ? <Link className="h-10 px-3 py-2 text-sm underline" href={siteConfig.routes.adminPerfumes}>Clear</Link> : null}</form>
    {perfumes.length ? <><div className="hidden overflow-hidden border border-jp-admin-border bg-jp-admin-surface md:block"><table className="w-full table-fixed text-left"><thead className="bg-[#f0ece5] text-[10px] uppercase tracking-[.14em] text-jp-text-secondary"><tr><th className="w-[43%] p-4">Perfume</th><th className="w-[14%] p-4">Variants</th><th className="w-[18%] p-4">Availability</th><th className="w-[17%] p-4">Homepage</th><th className="w-[8%] p-4 text-right">Action</th></tr></thead><tbody>{perfumes.map((perfume) => <tr key={perfume.id} className="border-t border-jp-admin-border"><td className="p-4"><Link className="flex items-center gap-3" href={siteConfig.routes.adminPerfume(perfume.id)}><ImageCell url={perfume.primaryImageUrl} /><span className="min-w-0"><span className="block truncate font-display text-2xl">{perfume.name}</span><span className="block truncate text-xs text-jp-text-secondary">{perfume.scentCharacters.join(" · ")}</span></span></Link></td><td className="p-4 text-sm">{perfume.variantCount} variants<br /><span className="text-xs text-jp-text-secondary">{perfume.totalQuantity} units</span></td><td className="p-4"><span className={perfume.isAvailable ? "bg-jp-status-confirmed px-2 py-1 text-xs font-semibold text-jp-status-confirmed-foreground" : "bg-jp-status-attention px-2 py-1 text-xs font-semibold text-jp-status-attention-foreground"}>{perfume.isAvailable ? `${perfume.availableVariantCount} available` : "Unavailable"}</span></td><td className="p-4 text-xs">{perfume.isFeatured ? <span className="mr-1 bg-jp-admin-action px-2 py-1 text-white">Featured</span> : null}{perfume.isBestseller ? <span className="border border-jp-admin-border px-2 py-1">Bestseller</span> : "—"}</td><td className="p-4 text-right"><Link className="text-sm font-semibold underline" href={siteConfig.routes.adminPerfume(perfume.id)}>Edit</Link></td></tr>)}</tbody></table></div><div className="grid gap-3 md:hidden">{perfumes.map((perfume) => <article className="border border-jp-admin-border bg-jp-admin-surface p-4" key={perfume.id}><div className="flex gap-3"><ImageCell url={perfume.primaryImageUrl} /><div><h2 className="font-display text-2xl">{perfume.name}</h2><p className="text-xs text-jp-text-secondary">{perfume.scentCharacters.join(" · ")}</p></div></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-xs text-jp-text-secondary">Variants</dt><dd>{perfume.variantCount} · {perfume.totalQuantity} units</dd></div><div><dt className="text-xs text-jp-text-secondary">Availability</dt><dd>{perfume.isAvailable ? "Available" : "Unavailable"}</dd></div></dl><Link href={siteConfig.routes.adminPerfume(perfume.id)} className="mt-4 inline-block text-sm font-semibold underline">Edit</Link></article>)}</div></> : <div role="status" className="border border-jp-admin-border bg-jp-admin-surface p-8 text-center"><h2 className="font-display text-3xl">{active ? "No perfumes match these filters." : "No perfumes yet."}</h2><p className="mt-2 text-sm text-jp-text-secondary">{active ? "Adjust or clear your filters to see the catalogue." : "Add the first perfume to begin the catalogue."}</p>{active ? <Link className="mt-4 inline-block underline" href={siteConfig.routes.adminPerfumes}>Clear filters</Link> : null}</div>}
  </section>;
}
